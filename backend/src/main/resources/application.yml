server:
  port: ${PORT:8080}
  error:
    whitelabel:
      enabled: true

spring:
  datasource:
    username: 'pracktika2021'
    password: 'KC8mmymg7vL3'
    url: jdbc:postgresql://practika2021.opencode.su:5454/postgres
    driver-class-name: 'org.postgresql.Driver'
  jpa:
    database-platform: 'org.hibernate.dialect.PostgreSQLDialect'
    hibernate:
      ddl-auto: 'create-drop'
    show-sql: true
  minio:
    url: https://play.min.io
    bucket: 00000qweqwe #Minio Bucket name for app
    access-key: #login
    secret-key: #password


# Конфигурация minio сервера изображений
minio:
  ip: #
  # minio пароль для входа в аккаунт
  accessKey: #
  secretKey: #

  ## Правила присвоения имен корзин (папок) должны соответствовать стандарту Amazon S3. Подробнее см. Http://docs.aws.amazon.com/AmazonS3/latest/dev/BucketRestrictions.html
  bucketName:
    ## Фото папка
    facility: facility-photos